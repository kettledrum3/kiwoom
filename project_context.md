# H2 Cost Averaging & Value Rebalancing [cavr] 개발 이력 기록

이 문서는 **CAVR** 프로젝트의 개발, 리팩토링 및 트러블슈팅 이력을 날짜별로 정리한 문서입니다. (전략 공식 및 방법론은 [strategy_formula.md](file:///d:/Python_D/kiwoom/strategy_formula.md)를 참고하십시오.)

---

## 1. 2026년 3월: 프로젝트 설계 및 백테스트 환경 구축

### 📅 2026-03-12 (최초 설계 및 구조화)
*   **설정/상태 분리 및 전략 엔진화**: 전략의 파라미터(`Config`)와 상태 데이터(`State`)를 `dataclass`로 설계하고, 각 전략 로직을 `CostAveragingEngine`과 `ValueRebalancingEngine`으로 캡슐화.
*   **금융 계산 정밀화**: 부동소수점 오차 방지를 위해 `Decimal` 자료형 적용 (T값 및 별값 계산).
*   **Broker 추상화**: 실전 투자와 백테스트에서 동일한 엔진 로직을 공유하기 위해 `Broker` 추상 클래스를 설계하고 `KiwoomBroker`와 `BacktestBroker`로 분화.
*   **시뮬레이션 UI**: Toga (BeeWare) 데스크탑 GUI 연동 시도.

### 📅 2026-03-13 ~ 03-14 (무한매수법 고도화 및 기술 스택 전환)
*   **무한매수법(CA) 로직 보강**: 누적 매수액 동기화 누락 수정 및 쿼터 손절 모드 상태 엔진 구현.
*   **대시보드 피봇 (Streamlit)**: BeeWare 데스크탑 개발의 복잡도와 Plotly 시각화 도구 활용 한계를 극복하기 위해 **Streamlit** 대시보드 구조(`dashboard.py`)로 기술 스택 전면 전환 결정.

### 📅 2026-03-15 ~ 03-16 (데이터 수집 및 VR 백테스트 구현)
*   **데이터 수집기**: OpenAPI 연동 일봉 데이터 수집기 구현 (`core/fetch_data.py`).
*   **밸류 리밸런싱(VR) 엔진**: 실력공식 및 2주 주기 적립/인출 스케줄을 포함한 VR 백테스트 시뮬레이션 구현.
*   **Plotly 시각화**: 이중 Y축(자산 평가액 및 주가 변동, 매매 지점 마커 표시) 그래프 적용.

### 📅 2026-03-18 ~ 03-19 (백테스트 정밀화 및 VR 버그 수정)
*   **원금 추적 및 POOL 사용 제한**: 적립/인출 현금 흐름을 반영한 Net Principal(실제 투입 원금) 추적 및 투자 방식별 POOL 사용 한도(75%, 50%, 25%) 자동 제한 기능 추가.
*   **VR 매도 미체결 버그 수정**: 목표 주식 평가액 계산 시 현금을 공제하지 않아 지정가 매도가 높게 잡히던 수식 오류를 수정하여 정상 매도 체결 확인.

### 📅 2026-03-20 ~ 03-21 (실전 API 연동 및 UI 개편)
*   **`KiwoomBroker` API 연동**: Mock 데이터를 실제 `requests` 기반 REST API로 대체 (현재가, 잔고, 예수금, 주문).
*   **API 예외 처리**: 미국 거래소 코드 자동 매핑 및 모의/실전 거래소 TR ID 분기 적용.

### 📅 2026-03-22 ~ 03-23 (자동화 스케줄링 및 DB 도입)
*   **`APScheduler` 구현**: 미국 정규장(09:30 ET) 개장에 맞춰 자동 가동되도록 스케줄러 설계.
*   **SQLite DB 및 WAL 모드 도입**: 동시성 제어 및 가용 데이터 보존을 위해 파일 기반 JSON에서 SQLite DB(`database.py`)로 상태 저장소 전면 개편.
*   **실시간 감시 체계**: WebSocket API(`core/ws_client.py`) 연동 실시간 체결 감시 및 텔레그램 알림 연동.

---

## 2. 2026년 4월: 실전 투자 안정화 및 기능 고도화

### 📅 2026-04-09 ~ 04-10 (수익 정산 고도화 및 인프라 구축)
*   **정산 로직 정밀화**: 매도 시 실제 수수료율/세율을 반영한 정산금액 계산 및 실시간 평단가 기반 실현 손익 정밀 산출 적용.
*   **이력 관리 및 보안**: 종료된 전략의 상태를 별도 테이블로 아카이빙하는 히스토리 시스템 구축. 일일 HTML 보고서 자동 발송(Zoho SMTP) 및 OTP 기반 사용자 인증(Auth) 도입.

### 📅 2026-04-13 ~ 04-15 (동적 관리 및 자동 차수 전환)
*   **대시보드 개선**: 로그인 프리징 해결, UI 상태 자동 복구 기능 추가.
*   **자동 차수 전환 (Cycling)**: 전량 매도 시 해당 차수를 자동 종료하고, 즉시 다음 차수 별칭을 생성하여 1회차 재진입 매수(지정가 오프셋)를 수행하는 로직 완료.

### 📅 2026-04-16 ~ 04-17 (데이터 정합성 및 예외 처리)
*   **불완전 주문 클리닝**: API 응답 필드 분석을 기반으로 수동 주문의 가격 누락을 교정하고, 이미 체결된 불완전 데이터를 정리하는 도구 추가.
*   **자원 최적화**: 장외 시간 API 호출 및 웹소켓 연결 차단하여 토큰 낭비 및 로그 노이즈 최소화.

### 📅 2026-04-19 ~ 04-20 (이력 분석 고도화 및 세금 체계 정교화)
*   **MDD 지표 구현**: 종료된 전략의 실현 수익 데이터를 기반으로 최대 낙폭(MDD)을 산출하도록 통계 고도화.
*   **시장별 세금/수수료 설정**: 한국 시장 수수료/세금(0.2038%) 및 미국 신규 계좌 수수료/SEC Fee 반영 로직을 `.env` 설정을 통해 시장별로 완벽히 격리.

### 📅 2026-04-21 ~ 04-24 (주문 계획 필터링 및 LOC 모사)
*   **중복 주문 방지**: 주문 계획 생성 시 이미 브로커에 제출된 주문(odno)이 있을 경우 계획에서 자동 필터링.
*   **LOC 모사 주문**: 한국 시장의 LOC 미지원 대응을 위해 15:10 지정가 일괄 제출 로직 구축. 자전거래 방지용 오프셋(음수)은 매수 시에만 적용하도록 원칙 수립.

### 📅 2026-04-27 ~ 04-30 (스케줄러 개선 및 웹소켓 예외 처리)
*   **운영 시간 확장**: Nextrade 및 프리/애프터 마켓 체결 누락 방지를 위해 감시 운영 시간 연장.
*   **웹소켓 예외 차단**: 중복 접속 에러 시 서버의 IP 차단을 피하기 위해 2분 대기 후 재접속하도록 지수 백오프 적용.

---

## 3. 2026년 5월: 최적화, 모니터링 강화 및 손익 분석 시스템

### 📅 2026-05-01 (휴장 대응 및 웹소켓 세션 유지)
*   **휴장 대응**: 주말 점검 및 해외 실시간 현재가 미변동 판정 알고리즘을 이용해 휴장일에는 시스템 대기 모드로 자동 전환됩니다.
*   **웹소켓 세션 유지**: PINGPONG 제어 메시지에 대한 에코(Echo) 응답 구현으로 5분 주기 끊김 현상 해결. 403 에러 연속 발생 시 텔레그램 긴급 경고 발송.

### 📅 2026-05-02 (장마감 전수 대조 및 취소 주문 관리)
*   **전수 대조 스케줄링**: 웹소켓 유실에 대응하기 위해 일일 보고서 발송 직전 모든 전략의 체결 상태를 REST API로 강제 동기화하는 `job_sync_and_report` 도입.
*   **취소 주문 관리**: 주가 조건 미달로 취소된 LOC 주문을 `CANCELED` 상태로 저장하고 매일 새벽 3일 경과 데이터를 자동 클린업.

### 📅 2026-05-04 (데이터 정규화 및 API 매핑 최적화)
*   **티커/주문번호 정규화**: API의 주문번호 앞자리 `0` 처리 및 국내 종목 코드의 `A` 접두사로 인한 동기화 불일치 해결.

### 📅 2026-05-05 (야간) (웹소켓 세션 충돌 방지 및 디버깅)
*   **세션 명시적 종료**: 한국/미국 웹소켓 간 세션 충돌을 방지하기 위해 각 정규장 마감 시 웹소켓을 명시적으로 close 처리.
*   **인프라 가이드**: API 문서를 `API_SPEC.md`로 정리.

### 📅 2026-05-06 (REST API 캐싱 및 스케줄러 2단계 분리)
*   **API 호출 부하 감소**: 실시간 시세 수신 시 반복되던 전일 종가 조회를 캐싱 처리하여 `RemoteDisconnected` 에러 방지.
*   **Prepare/Open 분리**: 장 시작 프로세스를 장 시작 3분 후(`Prepare`, 전일 체결 동기화 및 차수 전환)와 5분 후(`Open`, 당일 LOC 주문 제출)로 이원화하여 레이스 컨디션 차단.

### 📅 2026-05-07 (장중 급락 감시 고도화 및 순환 참조 해결)
*   **동적 기준가 급락 감시**: 장중 -3% 급락 매수 시 직전 체결가를 신규 기준으로 삼아 후속 하락을 감시하도록 수정.
*   **코드 리팩토링**: `core/cavr.py`와 `core/database.py` 간 순환 참조 해결을 위해 유틸성 함수들을 `core/utils.py`로 분리.

### 📅 2026-05-11 (전략 순차 실행 및 프리장 주문)
*   **스케줄러 순차화**: 다중 전략 실행 시 로그 혼선을 방지하기 위해 스케줄러 내 전략 실행 간 2분 대기 시간 적용.
*   **프리장 매도 주문**: 미국 프리마켓 개장 직후(07:05 ET)에 지정가 매도 주문을 제출하도록 자동화 구현.

### 📅 2026-05-13 (오전/오후) (로그 폭증 제어 및 프리장-정규장 병행 주문)
*   **로그 폭증 방지**: 변동성 장세에서 매수 한도 초과 로그 폭증을 차단하기 위해 `scheduler.log`에 `RotatingFileHandler`(2MB 제한)를 적용하고, 스킵 시 조기 반환하도록 로직 최적화.
*   **주문 병행 처리**: 프리마켓 지정가 매도 주문이 미체결 상태여도 정규장 LOC 주문 제출이 막히지 않도록 개별 주문 존재 여부를 체크하도록 수정.

### 📅 2026-05-14 ~ 05-15 (애프터마켓 오발주 방지 및 급락 매수 예산 산출)
*   **오발주 가드**: 주문 가능 시간을 정규장 종료 후 1분으로 제한하여 애프터마켓 오발주 원천 차단.
*   **비례 수량 매수**: 장중 급락 매수 시 고정 1주가 아닌, 1회 매수금의 절반(0.5T) 예산에 해당하는 비례 수량을 매수하도록 개편.
*   **통화 표기**: 한국 시장 텔레그램 알림 및 로그에 원화(₩) 기호 및 소수점 제거 포맷팅 완벽 적용.

### 📅 2026-05-18 (휴장 판정 강화 및 DB 기반 토큰 관리)
*   **휴장 판정 신뢰성**: 미국 휴장일 판정 시 20초 간격 3회 가격 변동 확인을 최대 4세트(중간 대기 1분 추가)까지 하도록 보강.
*   **DB 토큰 관리**: `api_tokens` 테이블을 설계하여 발급된 API 토큰을 DB에서 통합 관리하고, 14시간 경과 시 Soft Refresh 적용.

### 📅 2026-05-20 ~ 05-21 (주문 유형 표준화 및 웹소켓 파싱 디버깅)
*   **주문 유형 코드화**: `ORDER_TYPE_MAP`을 전면 도입하여 시스템 로그 및 알림 메시지에 주문 유형(지정가, LOC, MOC 등)을 명확히 명시.
*   **장외 웹소켓 차단**: 1006 에러 발생 시 정규장 외 시간일 경우 불필요한 재연결 중단 처리.

### 📅 2026-05-22 (주문 유형 정보 보완 및 손익 분석 탭 구축)
*   **실시간 체결 정보 보완**: 웹소켓 데이터 내 누적 주문 유형 코드가 누락된 경우 DB를 역조회하여 알림에 보완하는 로직 구현.
*   **손익분석 탭**: Streamlit 대시보드 내 일별/주간/월간 수익 추이 및 종목별 비중을 Plotly 차트로 시각화.
*   **월간 투자 결산 리포트**: 매월 1일 지난달 수익 곡선 그래프 이미지를 Matplotlib(`Agg` 백엔드)로 자동 생성하여 HTML 이메일에 임베드 발송하는 시스템 구현.

### 📅 2026-05-25 (보고서 환율 가시성 강화)
*   **환율 정보 추가**: 일일 보고서 이메일 및 대시보드 손익 분석 탭에 환율 변동 추이(원화, %) 그래프 추가 및 이메일 스타일 리뉴얼.

### 📅 2026-05-26 ~ 05-27 (KR 10시 진입 스케줄링 및 평단가 역추적)
*   **10시 진입 지연**: 한국 시장의 장 초반 급변동을 피하고 신규 차수 진입 오발주를 막기 위해 신규 진입 매수 스케줄을 10:00 KST로 지연 분리.
*   **환율 API 연동**: 환율 조회 제한 업데이트를 통해 환율 안정적 조회 보장.
*   **평단가 역추적 Fallback**: 전량 매도 후 즉시 차수가 전환되는 중 뒤늦게 체결된 매도 내역의 수익금 계산 시 평단가가 0으로 계산되는 오류를 방지하기 위해 DB 마지막 평단가를 역추적하는 로직 적용.

### 📅 2026-05-28 ~ 05-30 (웹소켓 레이아웃 및 알림 최적화)
*   **웹소켓 최종 안정화**: 텔레그램 알림 문구를 '주문상태' 및 '일시:'로 다듬고 미국 웹소켓 체결 시각 파싱 기능 개선.

---

## 4. 2026년 6월: 사용성 최적화 및 동시성 제어 강화

### 📅 2026-06-01 ~ 06-02 (대시보드 종목 편집기 및 API 조회 자동화)
*   **st.data_editor 기반 종목 관리**: 상품기본조회 API를 연동하여 티커 입력 시 종목명/유형을 자동 조회해 `ETF_list_*.txt`에 기록하고, 대시보드 상에서 리스트를 직접 수정/삭제할 수 있는 관리자 화면 구축.
*   **DateParseError 방지**: 수신 시각 접미사가 포함된 문자열을 pandas 날짜 형식으로 변환할 때의 정규화 전처리 로직 강화.

### 📅 2026-06-04 ~ 06-05 (TPS 제한 대응 및 대시보드 로컬 상태 최적화)
*   **API TPS 제한**: 잔고 조회 실패를 예방하기 위해 0.5초(이후 1.0초) 대기 후 최대 3회 재시도하도록 개선.
*   **로컬 변수/DB 활용**: 매수/매도 주문 시 반복 호출되던 잔고 조회 API를 제거하고 상태 엔진의 변수를 활용하여 API 부하 극대화 감소.
*   **LOC 모사 시간 단축**: 동시호가를 고려하여 LOC 모사 제출을 15:10으로 앞당기고 종목 간 발송 간격을 90초로 연장.

### 📅 2026-06-10 (TPS 잔고조회 예외 및 비동기 웹소켓 스레드 분리)
*   **TPS 조회 실패 가드**: 한국 10:00 점검 중 TPS 제한으로 잔고 조회가 실패한 경우를 '전량 매도'로 오판하여 강제 차수 전환되던 치명적 결함을 수정 (실패 시 None 반환 후 스킵).
*   **비동기 웹소켓 스레드 분리**: 웹소켓 수신 루프를 비동기 스레드 풀(`asyncio.to_thread`)로 분리하여 메인 이벤트 루프 블로킹을 원천 차단하고 1006 에러 해결.
*   **동시성 토큰 락**: 다중 스레드의 동시 토큰 발급 요청으로 발생하는 Rate Limit 에러를 예방하기 위해 `_token_fetch_lock` 및 5초 쿨다운 적용.

### 📅 2026-06-12 (시스템 가드 강화 및 V2.2 정밀 조정)
*   **Unpacking 에러 가드**: API 호출 실패 등으로 None 반환 시 TypeError 방지를 위해 방어 코딩을 전수 적용하고 10시 점검 시 기존 잔고 캐시 데이터를 활용하도록 안전 장치 마련.
*   **토큰 계좌 기준 공유**: 한국/미국 시장이 동일 계좌를 사용할 경우 `account_no` 기반으로 토큰 상태를 동기화하여 발급 제한 충돌 방지.
*   **V2.2 소수점 올림 패치**: 무한매수법 V2.2 가이드에 부합하도록 별값(Star%) 계산 시 소수점 넷째 자리에서 올림 처리하도록 로직 수정.
*   **스케줄 튜닝**: 전략 실행 대기 간격을 60초로 최적화하고, 미국 휴장 점검 구동을 현지 시간 기준 07:00~18:00로 제한.

---

## 5. 2026년 6월: 무한매수법 V4.0 정식 구현 및 토스증권 마이그레이션 피드백

### 📅 2026-06-14 (전략 수립 및 문서화)
*   **V4.0 방법론 정립**: 유동적 매수액 계산($\text{Pool} / (a-T)$), 쿼터 매도 시 T값 보정($T \times 0.75$), 소진 시 리버스 모드(5일 평균 별지점) 등 핵심 로직 문서화 완료.
*   **T값 계산 규칙 통일**: 모든 버전에서 T값 계산 시 소수점 반올림/올림을 제거하고 순수 계산값을 사용하도록 엔진 전수 수정.
*   **전략 가이드 업데이트**: `strategy_formula.md` 및 `README.md`에 V4.0 상세 수식 반영.
*   **V4.0 엔진 구현**: `core/cavr.py`에 버전별 분기 로직 및 리버스 모드(무한 매도, 쿼터 매수) 프로토타입 구현 완료.
*   **KR 리버스 모드 최적화**: 한국 시장 리버스 모드 첫날 MOC 주문을 시장가(01)로 매핑하고, 15:10 세션(LOC_ONLY)에서만 동작하도록 세션 필터 강화.
*   **브로커 API 연동 고도화**: 리버스 모드 기준가 산출을 위해 미국 및 한국 시장의 직전 5거래일 종가 평균 조회 기능 구현.
*   **실현 손익 보고 시스템 개편**: 대시보드 수익 요약 표에 차수별 역순 정렬을 적용하고, '총 매수액/매도액' 및 '이익률(%)' 지표를 추가하여 전략별 성과 분석 정밀화.
*   **데이터 복구 및 무결성 도구**: DB의 실현 손익 기록 오류를 해결하기 위해 거래 내역을 시간순으로 재추적하여 평단가와 이익을 일괄 재계산하는 `recalculate_all_profits_db` 기능 및 UI 추가.
*   **대시보드 UI 최적화**: 전략 카드 내 할당 예수금, 버전 정보, 목표 수익률 등 핵심 파라미터 노출을 강화하고 사이드바 입력 항목을 전략 타입(CA/VR)에 맞춰 동적으로 필터링하도록 수정.
*   **인프라 유연성 확보**: Docker 및 OCI 배포 환경에 대응하기 위해 `monitor_scheduler.py` 등의 경로 처리 및 환경 변수 로드 로직을 절대 경로에서 실행 파일 기준 상대 경로로 개선.

### 📅 2026-06-16 (스케줄 최적화 및 장중 급락 로직 보완)
*   **스케줄 및 트래픽 최적화**: 한국 시장 장 마감(15:50) 후 웹소켓 연결 시도를 완전 중단하도록 수정. 웹소켓 구독 요청 간 0.1초 지연 추가 및 장 시작 작업(`Prepare`, `Open`) 시간을 5~10분 지연시켜 개장 초기의 API TPS 부하와 연결 끊김 현상 완화.
*   **장중 급락 매수 안정화**: 초단위로 쏟아지는 시세 데이터로 인한 중복 매수를 방지하기 위해 `_ca_intraday_lock` 스레드 락 도입.
*   **상태 관리 정합성 확보**: 급락 매수 주문 성공 직후 상태(누적 매수액, 기준가)를 DB에 즉시 저장하여 감시 기준점(Baseline) 갱신 지연 문제 해결.
*   **한도 관리 강화**: 일일 급락 매수 한도(2T) 및 회당 매수액(0.5T)이 정확히 지켜지도록 중복 업데이트 로직을 제거하고 동시성 제어 강화.
*   **버그 수정**: `run_intraday_check` 내 미정의 변수(`baseline_drop_rate`)를 `trigger_drop_rate`로 수정하여 로깅 오류 해결.

### 📅 2026-06-17 (월간 투자 결산 리포트 가독성 개선)
*   **월간 투자 결산 리포트 오류 수정**: `generate_monthly_report_html`에서 생성된 `MIMEMultipart` 객체를 `send_email` 함수로 직접 전달하여 이메일 클라이언트가 HTML 본문과 임베드된 이미지를 올바르게 렌더링하도록 수정. `MIMEText` 객체에 `charset='utf-8'`을 명시적으로 지정하여 한글 인코딩 문제 방지.

### 📅 2026-06-19 (웹소켓 안정성 보완 및 장중 정기 동기화 백업 구현)
*   **웹소켓 클라이언트 주도 핑퐁**: 웹소켓 서버의 유휴 세션 끊김을 방지하기 위해 2분 간격으로 클라이언트에서 `PINGPONG` 프레임을 선제 송신하는 `_ping_loop`를 구현하고, 연결 종료 시 안전하게 태스크를 취소(Cancel)하도록 수명 주기 가드를 적용함.
*   **장중 매시간 45분 REST API 정기 동기화**: 웹소켓 연결 유실에 따른 데이터 누락을 메우기 위해 최근 1일(어제 및 오늘)의 체결 내역과 미체결 주문을 REST API로 강제 수집 및 DB 동기화하는 `job_hourly_market_sync` 백업 잡 신설.
*   **시장별 백업 스케줄 설정**:
    *   한국(KR): 장 운영 시간대인 **09:45 ~ 14:45 KST** 동안 매시간 45분마다 동작하도록 크론 트리거(`hour='9-14'`) 등록.
    *   미국(US): **09:45 ~ 16:45 ET** 동안 매시간 45분마다 동작하도록 크론 트리거(`hour='9-16'`) 등록.
    *   각 시장의 휴장일에는 백업 동기화가 동작하지 않도록 스킵 가드 설정.

### 📅 2026-06-23 (장중 급락 매수 로직 개선 및 급락 기준 변경)
*   **장중 급락 매수 지연 실행 및 시장가 전환**: 급락 감시 중 변동성 장세에서 예기치 않게 동일 종목을 단시간 내 중복 매수하는 문제를 방지하기 위해, 급락 감지 시 즉시 매수하지 않고 펜딩 플래그(`intraday_drop_pending`) 설정 및 기준가(`last_execution_price = current_price`)를 DB에 즉시 업데이트한 후 10초 뒤 시장가(`price_type="01"`)로 0.5T 분량을 매수하는 지연 매수 로직을 구현함.
*   **자가 치유형 펜딩 플래그**: 프로세스 비정상 종료 등으로 인해 플래그가 펜딩 상태로 고정되는 것을 방지하기 위해 60초 초과 시 자동으로 해제되도록 안전 장치 적용.
*   **급락 기준 변경 (3% -> 5%)**: 한국 시장 및 미국 시장 모두 급락 대응 기준 비율을 기존 3%에서 **5%**(`intraday_drop_threshold=0.05`)로 상향 조정하고 동적 구성이 가능하도록 `CAConfig`에 필드 추가.
*   **시장별 시장가 주문 처리**:
    *   한국: 시장가 주문(`01`) 시 주문 가격을 `0`으로 변환하여 제출하도록 보완.
    *   미국: 시장가 주문(`01`) 시 주문 가격을 `0.0`으로 변환하여 `OVRS_ORD_UNPR`을 `"0.00"`으로 전달하도록 보완.

### 📅 2026-06-26 (동기화 알림 추가, 보고서 예수금 표시, 전략할당금액 수정)
*   **체결내역확인 동기화 알림**: 매시 45분 정기 동기화(`job_hourly_market_sync`) 성공 시 신규 복구 및 업데이트 건수 요약을 텔레그램 메시지로 발송하도록 추가함. `sync_trade_history_db`가 동기화된 개수를 반환하도록 수정함.
*   **일일 보고서 예수금 표시**: 일일 운용 보고서 HTML 생성 시 브로커를 통해 실제 계좌 예수금을 조회하여 보고서 상단에 시각적으로 표시하도록 개선함.
*   **전략할당금액 버그 수정**: 대시보드 "현재 계좌 상태 조회" 및 VR 엔진(`run_cycle`) 내에서 개별 전략의 가상 할당 자금(`pool`)을 실제 계좌 전체 예수금으로 강제 덮어쓰던 오류를 해결함 (실전 투자 시 덮어쓰지 않도록 수정).
*   **Docker 빌드 오류 및 해시 불일치 해결**: PyPI 패키지 다운로드 중 네트워크 타임아웃 및 이로 인한 불완전 다운로드로 해시 불일치 오류가 발생하는 문제를 해결하기 위해, `dockerfile` 내에서 `pip`를 먼저 최신 버전으로 업그레이드하고 공식 PyPI 서버 기준 `--default-timeout=1000`, `--retries 10`으로 안전하게 다운로드하도록 보완함.
*   **웹소켓 PING 루프 AttributeError 해결**: `websockets` 라이브러리 버전에 따라 `ClientConnection` 객체에 `open` 어트리뷰트가 없어 발생하는 오류를 해결하기 위해, `hasattr` 체크를 거쳐 `websocket.open` 또는 `not websocket.closed`를 안전하게 사용하도록 핑 루프(`_ping_loop`) 검사 로직을 보완함.

### 📅 2026-06-27 (예수금 표시 명확화, V4.0 유동 매수금 계산 보정, 환율 하루 2회 갱신 및 기준점 보존)
*   **전략 예수금 고정 및 분리**: 실시간 체결 처리 시 무한매수법(CA)의 `ca_state.pool`을 직접 차감/가산하는 코드를 제거하여, DB에 저장된 최대 설정 한도(`pool`, 예: SOXL $16,000)를 초기 상태 그대로 영구 보존함.
*   **대시보드 실시간 예수금 계산 및 캐시**: 
    *   대시보드 상의 `💰 전략 할당 예수금`에 DB의 고정된 설정 예수금에서 현재 주식 투입액을 차감한 실시간 가용 예수금 `s_pool = pool - (shares * avg_price)`을 계산해 보여주도록 수정함.
    *   `🏦 거래소 총 예수금`에 실제 계좌 예수금을 매핑하되, 5초 Fragment 주기마다 API를 직접 호출하지 않고 수동 계좌조회 버튼 클릭 시 세션에 캐시된 값을 활용하도록 수정하여 403 API 트래픽 차단 위험을 완화함.
*   **무한매수법 V4.0 유동 매수금 공식 보정**: `core/cavr.py`에서 V4.0 유동 매수금 계산 식에 쓰이던 분자 인자를 고정된 `pool` 대신 실제 가용 잔액 `s_pool`로 대체하고 매일 정상 갱신되도록 보완함.
*   **미국 정규장 시간 기준 하루 2회 환율 갱신**: 뉴욕 시간대(`ny_tz`) 기준 **07:30 ET(장 시작 2시간 전)** 및 **16:05 ET(장 마감 직후)** 두 개의 CronJob으로 분리하여 환율을 하루 두 번 업데이트하도록 개편함. (서머타임 변동 자동 대응)
*   **환율 전일대비 변동 및 기준 환율 보존**: 
    *   환율 갱신 시 DB config의 `USDKRW_BASE_RATE`(전일 마감 환율)와 비교하여 변동폭(`diff`) 및 등락률(`pct`)을 직접 수동 계산해 기록하도록 보완함.
    *   장 마감 직후(16:05 ET) 업데이트 완료 시에만 오늘 환율을 새로운 `USDKRW_BASE_RATE`로 저장하여, 전일 정규장 마감 시점의 환율이 항상 정확한 전일대비 기준점으로 보존되도록 구현함.
    *   대시보드 화면 현재 환율 표시 위젯에도 이 변동폭(delta)이 정상 표기되도록 수정함.

---

## 6. 2026년 7월: 키움증권 REST API 전면 마이그레이션 완료

### 📅 2026-07-05 (키움증권 REST API 마이그레이션 및 토스 개량사항 이식)
*   **키움증권 REST API 전면 마이그레이션**:
    *   기존 한국투자증권(KIS) API 기반의 자동매매 엔진을 **키움증권 REST API** 환경으로 완전히 마이그레이션 완료.
    *   공통 인증 토큰 발급 API `au10001`에 맞춰 `get_access_token` 및 토큰 무효화 로직 재설계. `expires_dt` (KST 기준)를 통한 만료 1시간 전 안전 갱신 연동.
    *   `kis_headers` 함수 내부 브릿지 처리를 통해 키움증권 REST API의 헤더 규격(`api-id`, `authorization` Bearer 토큰)을 자동 반환하도록 수정하여 비즈니스 엔진 레이어 수정을 최소화.
    *   국내주식(`ka10001`, `ka10081`, `kt00004`, `kt10000`/`kt10001`, `kt00007`) 및 미국주식(`usa06012`, `ust21070`, `ust21160`, `ust20000`/`ust20001`, `ust31301`) TR 사양 완벽 매핑.
    *   SOXL 등 NYSE Arca 상장 주식의 거래소 매핑 오차를 `stex_tp="NY"`로 보정하여 정상 응답 확보.
*   **실시간 웹소켓(FID) 이식**:
    *   KIS 웹소켓 특유의 복잡한 AES256 복호화 코드를 걷어내고, 키움의 JSON 포맷 실시간 체결(`00`, `F5`) 및 실시간 시세(`0B`, `FE`) 수신 및 FID 파싱 규격으로 단순화 연동.
*   **Toss 개량사항 이식**:
    *   **예수금 모니터링 및 자동 일시정지 (`job_monitor_deposits`)**: 스케줄러 내에 4시간 주기의 예수금 점검 작업을 탑재하여 가용 예수금 부족 시 텔레그램 경고 후 시스템을 일시정지(`paused`)하며, 보유 주식이 있어 매도 처리가 필요한 경우 구동을 지속하는 예외 처리를 반영.
    *   **미국 주식 Ticker 접두사 `'A'` 훼손 버그 수정**: 한국 주식의 Ticker 포맷상 접두사 `'A'`가 붙는 규칙(`clean_symbol.startswith('A') and len(clean_symbol) == 7 and clean_symbol[1:].isdigit()`)일 때만 첫 글자를 떼어내도록 정규 표현식을 보완하여 미국 주식(`AAPL`, `AMZN` 등)의 첫 글자가 지워지던 데이터 훼손 결함을 해결.
*   **수집기 마이그레이션 (`core/fetch_data.py`)**:
    *   과거 KIS API 클라이언트를 제거하고 키움 브로커의 `fetch_daily_chart` 를 불러 시뮬레이션용 CSV 데이터를 안정적으로 수집하도록 마이그레이션 완료.
*   **데이터베이스 마이그레이션 버그 패치 (`core/database.py`)**:
    *   DB가 없는 최초 기동 시 `strategy_state` 테이블 RENAME 에러로 강제 종료되던 OperationalError를 예외 처리 조건 분기로 정상화하여 안전성 강화.
*   **KIS 레거시 잔재 소거**:
    *   기존 KIS용 브로커 파일(`kis_kr.py`, `kis_us.py`) 및 API 명세 데이터 엑셀/CSV 파일, 토큰 캐시 파일 등을 프로젝트에서 완전히 제거하고 레거시 `open-trading-api-main/` 샘플 폴더를 삭제하여 키움증권 단일 브로커 아키텍처로 정립.

### 📅 2026-07-06 (키움CAVR 브랜딩 개편 및 키움 고시환율 조회 연동)
*   **키움CAVR 통합 브랜딩 개편**:
    *   인증번호, 가입 안내, 비밀번호 재설정 등의 시스템 자동 발송 이메일 제목 및 본문 내 `[CAVR]`, `CAVR 자동매매 시스템` 등의 표기를 모두 **`[키움CAVR]`**, **`키움CAVR 자동매매 시스템`**으로 변경하여 정체성을 통일함.
    *   일일 보고서 및 월간 결산 보고서의 이메일 말머리를 `[키움]`에서 **`[키움CAVR]`**로 전면 통일.
    *   대시보드(`dashboard.py`) 탭 명칭(`키움CAVR Dashboard`), 로그인 헤더, 사이드바 컨트롤 패널 타이틀을 **`키움CAVR`**로 보정하여 키움증권 전용 브랜딩 명확화.
    *   스케줄러 기동 및 텔레그램 시작 안내 메시지에 **`키움CAVR`** 명칭 부여.
*   **키움증권 고시환율(`ust31301`) 조회 공식 연동**:
    *   기존 외부 API(`exchangerate.host`) 찌르는 로직을 전면 제거하여, API Key가 누락된 환경에서도 무의미한 에러 알림이 차단되도록 설계.
    *   `KiwoomUsBroker.get_exchange_rate()`의 `ust31301`을 통한 `aplc_exrt`("적용환율") 수동 데이터 파싱 및 DB 바인딩 연동 완료.
*   **향후 작업 계획 (모의투자 선택 옵션)**:
    *   실계좌 외에 모의계좌 연동에 대응하기 위한 계좌별 조회/주문 분기 로직 및 모의투자 운영 여부 선택사항 추가 예정. (2026-07-07 구현 완료)

### 📅 2026-07-07 (모의투자 MOCK 계좌 지원 및 대시보드 UI/백테스트 연동)
*   **환경 변수 및 계좌 설정 통합**:
    - `MOCK_` 접두사를 가지는 모의투자용 Key 및 계좌번호를 `env/.env` 파일에 통합 수록하여 `.env.kr` / `.env.us` 파일의 중복 설정을 제거하고 관리 일관성을 제고함.
*   **거래 운영 모드(REAL / MOCK) 통합 분기**:
    - `core/database.py` 내부에 `get_trade_mode() -> str` 함수를 구현하여 순환 참조 없이 전역적으로 거래 모드를 읽을 수 있도록 지원.
    - `core/cavr.py` (`get_access_token`), `core/brokers/kiwoom_kr.py`, `core/brokers/kiwoom_us.py` 등 API 호출 및 브로커 인스턴스 초기화 시 `TRADE_MODE`에 따라 계좌 번호, API Key, Base URL 등을 실전/모의투자에 맞춰 동적으로 전환하도록 수정.
*   **웹소켓 실시간 체결 모의투자 대응**:
    - `core/ws_client.py` 생성자에서 거래 모드가 `"MOCK"`인 경우 `MOCK_SOCKET_URL` 및 `MOCK_CLIENT_ID`로 소켓 세션을 연결하도록 개선.
*   **대시보드 UI 연동 및 캐시 안전성 확보**:
    - 사이드바 최상단에 "실전 투자 (REAL)" / "모의 투자 (MOCK)" 선택 Selectbox를 추가하여 `TRADE_MODE` 시스템 설정을 빠르게 갱신하고 화면을 Rerun할 수 있도록 구현.
    - 브로커 인스턴스 캐싱 함수(`get_active_broker_instance`)의 캐시 키에 `mode`를 추가하여 모드 변경 시 캐시 충돌을 원천 차단.
*   **백테스트 편의성 및 모의 수수료 연동**:
    - 대시보드의 백테스트 탭에 "모의투자 수수료 기준 적용" 체크박스 옵션을 구현하여, 활성화 시 키움증권 모의투자 기본 수수료(국내 0.35%, 해외 0.30%)가 자동으로 세팅되어 편리하게 백테스트 시뮬레이션을 진행할 수 있도록 반영.
*   **향후 작업 계획**:
    - 다가오는 주말에 모의투자 백테스트 모드를 통해 최종 시스템 안정성과 시나리오를 점검 및 모니터링할 예정.
    - 모의 검증 완료 이후, 기존 한국투자증권(KIS) 레거시 프로그램을 완전히 종료(Shutdown)하고 키움증권 단일 엔진으로 전면 이관 및 가동 개시할 계획.
*   **Poetry 의존성 관리 및 패키징 모드 최적화**:
    - `requirements.txt` 의존성을 `pyproject.toml`에 동기화 완료 (`websockets`, `pycryptodome`, `matplotlib`, `urllib3`, `tabulate` 추가).
    - `poetry install` 시 로컬 폴더 패키지 미검출 에러 해결을 위해 `pyproject.toml` 하단에 `[tool.poetry] package-mode = false` 설정을 주입함.
*   **Docker 이미지 빌드 & Portainer 배포 설계**:
    - WSL Ubuntu 환경에서 `poetry.lock` 락킹 및 export 플러그인(`poetry-plugin-export`)을 활용하여 `requirements.txt`를 동기화하고 Docker 이미지를 빌드 및 Docker Hub(`simji3/kiwoom-*`)에 업로드하는 워크플로우 가이드라인([wsl_docker_build_guide.md](file:///C:/Users/simji/.gemini/antigravity-ide/brain/cc813e4e-d8f7-4d6f-8134-1320b387b362/wsl_docker_build_guide.md)) 정립.
    - OCI 가상머신의 Portainer Web Editor(Stack) 환경에서 Docker Compose YAML 스택 코드를 붙여넣기 하여 손쉽게 원클릭 배포가 가능하도록 환경 구성 및 볼륨 구조 정의.
*   **Git & GitHub 버전 관리 및 보안 연동**:
    - 로컬 Git 저장소 초기화 및 GitHub 원격 저장소(`kiwoom`) 연동 절차 가이드라인([git_github_usage_guide.md](file:///C:/Users/simji/.gemini/antigravity-ide/brain/cc813e4e-d8f7-4d6f-8134-1320b387b362/git_github_usage_guide.md)) 작성.
    - 소스 코드 푸시 전 `.gitignore` 설정을 정밀 점검하여 `env/` 디렉토리, `.env` 환경 변수 파일, `data/` 내부 SQLite DB 파일 등 민감한 계좌 정보 및 보안 데이터가 원격지에 절대 업로드되지 않도록 사전 차단 완료.

### 📅 2026-07-12 (대시보드 UI 대대적 보완, KeyError 차단 및 동기화 완비)
*   **사이드바 2x2 라디오 버튼 제어판 통합**:
    - 기존 드롭다운식 선택창들과 분산 배치된 전략 제어 체크박스들을 하나의 깔끔한 테두리가 있는 표 형태(`st.container(border=True)`)의 2x2 라디오 버튼 그리드로 개편.
    - **1행**: 운영 모드(실전 투자 / 모의 투자) & 활성 시장(미국 시장 / 한국 시장)
    - **2행**: 방법론(CA / VR) & 실행 모드(투자 실행 / 백테스트)
    - 수동/자동 구분용 주문 모드(manual/auto) 및 글로벌 자동매매 스위치 체크박스는 자동화 완성에 따라 불필요하므로 완전히 제거하여 UI 간소화 완료.
*   **대시보드 헤더 및 탭의 모의/실전 동기화 완비**:
    - 사이드바 운영 모드(실전/모의)가 변경되면 메인 대시보드 헤더의 타이틀(`st.title`)에 실시간으로 **`🚀 모의 투자 - CA/VR 전략`** 또는 **`🚀 실전 투자 - CA/VR 전략`**으로 동기화되어 출력되도록 타이틀 포맷팅 조건문 복원.
    - 미국/한국 시장 현황 탭을 동시에 모두 노출하되, 현재 활성화된 시장의 탭 명칭 앞에 체크마크 아이콘(`✅`)을 붙이고 **항상 첫 번째 탭(index 0)으로 순서를 동적으로 스왑**하도록 탭 렌더링 공식 보완. `st.tabs`에 유니크한 `key`를 매핑하여 시장/모드 전환 시 인덱스가 고정되어 화면이 멈추던 Streamlit 프레임워크 한계 해결.
*   **신규 전략 진입 시 `KeyError: 'shares'` 예방 조치**:
    - DB에 기존 정보가 없거나 캐싱된 `live_account` 데이터의 세션 껍데기(예: `broker_cash`만 들고 있고 종목은 없는 상태)만 있을 때 발생하던 `KeyError: 'shares'` 예외 해결.
    - `st.session_state['live_account'].get('symbol') == symbol`을 검사해 현재 종목 정보가 존재할 때만 계좌 연동을 수행하도록 방어 가드를 세우고 모든 딕셔너리 접근을 안전한 `.get()` 메서드로 전환.
*   **설정 탭 내 사용자 정보 매핑 및 DB 동기화**:
    - 설정 탭의 사용자명 기본값으로 하드코딩된 `"Investor"` 대신 실제 로그인 세션에 활성화된 사용자 이름(`st.session_state.username`, 예: `"kiwoomcavr"`)이 디폴트값으로 나타나도록 매핑.
    - 사용자명 수정 저장 시 `system_config` 뿐만 아니라 `user_auth` DB 테이블의 실제 사용자 레코드까지 동기화되어 업데이트되도록 구현.
*   **Portainer Stack 배포 가이드라인 보완**:
    - OCI 서버 터미널 CLI 방식 대신 웹 브라우저에서 Portainer Stack Editor를 열고 **"Re-pull image"** 옵션을 켜서 스택을 업데이트 및 재부팅하는 편리한 OCI 컨테이너 동기화 워크플로우를 정리 및 안내서([walkthrough.md](file:///C:/Users/simji/.gemini/antigravity-ide/brain/99e59c79-175c-4d89-9b23-ec3a00b2b1fb/walkthrough.md))에 등재.

### 📅 2026-07-13 ~ 07-14 (모의투자 시간대 분리, 미국 공휴일 판단 최적화 및 웹소켓 세션 갱신 고도화)
*   **모의투자 모드(`MOCK`) 전용 거래/웹소켓 운영 시간대 제한**:
    - 모의계좌의 특성상 미국 프리마켓 및 애프터마켓 거래가 지원되지 않는 환경을 고려하여, 미국 정규장 운영 시간대(09:30 ~ 16:01 ET)에만 주문 제출 및 실시간 웹소켓 세션 유지가 가능하도록 스케줄러([scheduler.py](file:///d:/Python_D/kiwoom/core/scheduler.py)), 웹소켓 클라이언트([ws_client.py](file:///d:/Python_D/kiwoom/core/ws_client.py)), 대시보드([dashboard.py](file:///d:/Python_D/kiwoom/dashboard.py)) 내 운영 감시 시간대를 모의투자 모드 분기에 따라 제한함.
    - 모의투자 시 프리마켓 지정가 매도 스케줄(`job_us_pre_market_limit_sells`)이 동작하지 않도록 예외 차단 추가.
*   **미국 공휴일 판정 범위 확장 및 API 부하 차단 ([cavr.py](file:///d:/Python_D/kiwoom/core/cavr.py))**:
    - `is_us_market_holiday`에 6월 준틴스, 7월 독립기념일, 9월 노동절, 11월 추수감사절, 12월 크리스마스의 대체 휴일 판정 규칙을 구현하여 휴장 점검 신뢰성을 대폭 보강함.
    - 기존에 20초 간격으로 `KiwoomUsBroker` API를 통해 TQQQ 시세를 반복 조회하여 개장 여부를 체킹하던 비효율적인 가격 변동 감지 방식(API 트래픽 소모 방식)을 전면 폐기하고, 오프라인 달력 기반 공휴일 로직과 사용자 정의 휴장일(`load_custom_holidays`) 비교만으로 안전하게 판정하도록 개선.
*   **웹소켓 예외/인증 대응 및 견고화 ([ws_client.py](file:///d:/Python_D/kiwoom/core/ws_client.py))**:
    - **URL 자동 보정**: `ws_url` 끝부분에 각 시장별 웹소켓 엔드포인트 경로(`/api/dostk/websocket` 또는 `/api/us/websocket`)가 누락되었을 시, 자동으로 이를 검출하여 규격에 맞게 덧붙여 연결하도록 자동 보정 로직 구현.
    - **토큰 강제 재발급 및 세션 복구**: 실시간 세션 연결 중 웹소켓 서버로부터 로그인 세션 불일치나 만료 오류 코드(`100013`, `100018`, `100004` 등) 수신 시, 즉시 `_force_token_renew` 플래그를 올리고 연결을 차단(close). 이후 재연결 시 `get_access_token`에 `force=True`를 전달하여 확실히 API 토큰을 재생성하고 재연결을 수립하도록 세션 자동 복구 프로세스 구현.
    - **기본 종목 구독 Fallback**: 시스템 내 활성화된 감시 전략이 없을 때, 에러 방지를 위해 환경 변수 `ticker` 또는 기본 설정 종목(KR: `122630`, `005930`, US: `SOXL`, `TQQQ`)을 자동으로 구독하도록 가드 적용.
    - **구독 등록 규격 명시**: 체결 및 실시간 등록 데이터 전송 시 JSON 명세의 `"trnm": "REG"` 필드를 항상 누락 없이 포함하도록 수정.

### 📅 2026-07-25 (한국/미국 모의투자 계좌 정보 분리 적용 및 검증)
*   **시장별 모의투자 계좌 정보 분리**: 
    - 키움 모의투자 한국주식(`MOCKKR_`)과 미국주식(`MOCKUS_`)의 모의계좌번호 및 API Key가 다르게 설정되어 있음에 따라, 환경 변수를 정확히 구분하여 읽도록 개선함.
*   **공통 모듈 및 브로커 파일 수정**:
    - `core/cavr.py` (`get_access_token`): `market` 파라미터가 `"KR"`일 때는 `MOCKKR_` 계열 변수들을, `"US"`일 때는 `MOCKUS_` 계열 변수들을 동적으로 읽어오도록 수정.
    - `core/ws_client.py` (`KisWebSocketClient.__init__`): `trade_mode == "MOCK"`인 경우 시장(`self.market`)에 맞추어 `MOCKKR_CLIENT_ID` 또는 `MOCKUS_CLIENT_ID`를 할당하도록 수정.
    - `core/brokers/kiwoom_kr.py` / `kiwoom_us.py`: 생성자에서 `MOCK_ACCOUNT_NO` 대신 각각 한국 모의투자 전용 `MOCKKR_ACCOUNT_NO` 및 미국 모의투자 전용 `MOCKUS_ACCOUNT_NO`를 사용하도록 수정.
*   **테스트 검증 완료**:
    - `test_kiwoom.py` 스크립트를 통해 한국 모의계좌(`8129902310`) 및 미국 모의계좌(`6111091910`)의 토큰 발급과 예수금 조회 등 API 연동이 시장별 계좌번호에 맞춰 각각 성공적으로 작동함을 검증함.

### 📅 2026-07-27 (웹소켓 로그인 인증 에러 보완 및 이벤트 기반 구조 개선)
*   **웹소켓 로그인 인증(체결 통보 등록 완료) 후 시세 구독 적용**:
    - 웹소켓 연결 성공 직후 체결 통보 구독(api-id `"00"`/`"F5"`)을 발송한 뒤 로그인 인증 완료 응답(Control Message)을 수신하기 전에 시세 구독(api-id `"0B"`/`"FE"`)을 거의 동시에 발송하여 서버 측 세션 오류(`100013`)로 연결이 끊어지는 구조적 원인을 해결함.
    - `core/ws_client.py`의 `connect` 메서드에서 `subscribe_prices` 즉시 호출을 제거함.
    - 클래스 초기화(`__init__`) 및 웹소켓 연결 진입 시 실시간 시세 구독 여부를 상태값으로 추적하는 `self.is_subscribed_prices = False` 플래그를 추가함.
    - `on_message` 내 Control Message 수신 로직(`trnm == "REG"`)에서 체결 통보 성공 응답(에러가 없거나 성공 코드인 경우)을 이벤트 기반으로 감지하여 안전하게 `subscribe_prices`를 비동기 태스크(`asyncio.create_task`)로 트리거하도록 변경함.
*   **운용 전략 부재 시의 웹소켓 연결 유지 보강**:
    - 최초 로그인 시에 사용자가 지정한 전략이 없거나 대시보드 조작으로 인해 운용 전략이 일시정지/종료되어 활성 전략이 없는 경우에도 에러 없이 기본종목(`005930`, `122630`)을 정상적으로 시세 구독하여 웹소켓 세션을 끊김 없이 유지하도록 가드 처리함.
*   **환율 조회 이중화 및 Fallback 구조 구현**:
    - 키움증권 모의투자 환경의 지연 환율 고정 문제(1,455원)로 인한 실시간 시장 환율과의 괴리(1,470원)를 해결하기 위해 외부 API 연동을 도입함.
    - `core/brokers/kiwoom_us.py`의 `get_exchange_rate` 메서드를 개편하여, `env/.env.us`에 지정된 `EXCHANGERATE_KEY`를 활용해 `exchangerate.host` API를 통해 실시간 환율을 우선 수집함.
    - 외부 API 장애, 쿼터 제한 또는 잘못된 키 입력 등 오류 발생 시에도 시스템이 멈추지 않고 즉시 기존 키움증권 `ust31301` API를 연동하여 동작하는 Fallback 예외 처리를 구성하여 환율 정보 획득의 고가용성을 확보함.
*   **환율 API 일일 호출 3회 제한 및 초과 시 DB 환율 반환**:
    - `exchangerate.host` API의 무료 한도(월 100회) 소진을 예방하기 위해, 미국 동부 표준시(`America/New_York`) 날짜 기준 하루 최대 3회까지만 외부 API 갱신을 허용하도록 제한 기능을 도입함.
    - 3회 초과 호출 요청(하루 4회째 이상) 시 외부 API와 키움 API를 모두 호출하지 않고, DB(`system_config` 테이블)에 이미 갱신 저장된 오늘 자 환율(`USDKRW`)을 조회하여 즉시 반환하도록 함.
    - DB에 오늘 자 환율 정보가 유실되는 등 예외 상황 발생 시에만 안전하게 기존 키움 API로의 Fallback을 연동하여 시스템 고가용성을 유지함.
*   **웹소켓 장애 시 API 보호(토큰 락 방지) 및 전일종가 DB 캐시 복구 도입**:
    - 키움 모의투자 해외 웹소켓 서버의 일시적 장애 또는 계정 권한 등으로 인해 세션 오류(`100013`)가 3회 이상 무한히 지속될 경우, 기존의 정상 발급된 토큰까지 강제로 무효화하던 `self._force_token_renew = True` 플래그 설정을 억제함.
    - 이를 통해 웹소켓이 끊겨있는 동안에도 이미 획득한 유효 토큰을 안전하게 유지하여 백그라운드 스케줄러가 REST API를 통해 미국 주식 현재가 폴링 조회 및 전략 주문을 안정적으로 처리할 수 있도록 보호함.
    - `KiwoomUsBroker.get_previous_close(symbol)` 가 외부 API 장애나 락 충돌로 인해 전일종가를 로드하지 못할 시 `0.0`을 반환하지 않고, DB(`system_config` 테이블)에 기저장된 이전 캐시 전일종가를 즉시 복구해 반환하는 장애 복구 가드를 설계해 무한매수법 연산 중의 `ZeroDivisionError` 및 수식 깨짐 오류를 방어함.